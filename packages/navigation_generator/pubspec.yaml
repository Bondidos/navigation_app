name: navigation_generator
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  source_gen: ^1.4.0
  build: ^2.4.0
  analyzer: ^6.0.0
  code_builder: ^4.8.0
  dart_style: ^2.3.0

  navigation_api: ^0.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0

flutter:
